#include<bits/stdc++.h>
using namespace std;

typedef struct comple{
	double a,b;
}complex;

double mode(complex c){
	return sqrt( (double)(c.a*c.a) + (double)c.b*c.b );
}

complex divide(complex a ,complex b){

	complex c ;
	c.a = ( (a.a*b.a) 

}

int main(){
	
}